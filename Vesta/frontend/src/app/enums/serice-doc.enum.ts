export enum DocumentType {
  service = 0,
  maintenance = 1,
  instalation = 2,
  modernization = 3,
  disassembly = 4
}

export enum DocumentState {
  new = 0,
  waiting = 1,
  inProgress = 2,
  ended = 3
}

export enum DocumentPriority {
  critical = 0,
  high = 1,
  medium = 2,
  low = 3
}

